import { Routes } from '@angular/router';
import { ReactivePlaygroundComponent } from './components/reactive-playground/reactive-playground';

export const APP_ROUTES: Routes = [
  {
    path: 'reactive-playground',
    component: ReactivePlaygroundComponent,
  },
 
];
