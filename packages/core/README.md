# listedbase

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build listedbase` to build the library.

## Running unit tests

Run `nx test listedbase` to execute the unit tests via [Vitest](https://vitest.dev/).
